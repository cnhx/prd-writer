【项目：prd-writer】nmem-cli skill 与 Nowledge Mem MCP 修复
结果：已将 `/Users/bhua/.agents/skills/nmem-cli/SKILL.md` 从 0 字节空文件替换为带 YAML frontmatter 的 `nmem-cli` 0.8.3 兼容 skill；`nmem-cli` pipx 包已从 0.7.6 升级到 0.8.3；`~/.codex/config.toml` 已配置远程 Nowledge Mem MCP，Codex 启动验证不再报 nmem-cli skill 错误或本地 14242 MCP 连接错误；已用 `nmem --json wm read`、`nmem --json m add/show/search/delete` 完成读写检索端到端验证，临时测试 memory 已删除。
相关文件：/Users/bhua/.agents/skills/nmem-cli/SKILL.md；/Users/bhua/.agents/skills/nmem-cli/.backup/；/Users/bhua/.codex/config.toml；/Users/bhua/.local/pipx/venvs/nmem-cli
经验教训：`nmem-cli` skill 报错和 `nowledge-mem` MCP 连接失败是两个独立问题；先修 skill frontmatter，再用 `nmem config mcp show --host codex` 覆盖插件默认本地 MCP。
检索标签：#nmem #nowledge-mem #codex #skill #mcp #prd-writer

【项目：prd-writer】PRD 技术细节降级与异常流程补强
结果：已在 `write-prd` 增加 implementation detail policy、产品方案替代技术方案、条件表规范、异常流程覆盖和专项 cleanup passes；已在 `prd-score` 增加 implementation leakage、atomic pressure、condition consolidation、exception coverage 四个 Ready-to-Dev 门槛；根入口和 README 已同步设计规则，并同步到当前运行时安装目录 `/Users/bhua/.agents/skills/prd-writer`。
相关文件：write-prd/SKILL.md；prd-score/SKILL.md；SKILL.md；README.md；/Users/bhua/.agents/skills/prd-writer
经验教训：PRD 默认应写产品语义契约，Redis、数据表、队列、框架、原子化模块等实现细节需要降级为工程 follow-up；复杂判定要集中成表，核心流程必须包含异常与恢复路径。
检索标签：#prd-writer #write-prd #prd-score #implementation-leakage #exception-flow
